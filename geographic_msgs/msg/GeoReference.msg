# Geographic reference point, using the WGS 84 reference ellipsoid.

Header header            # stamp specifies time
string child_frame_id    # name of the tf frame for which the
                         # reference pose is valid
GeoPose pose             # the geographic pose
